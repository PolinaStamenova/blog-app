<h3>Post a Comment</h3>

<%= form_with model: [@post, @post.comments.build], class: "form" do |f| %>
    <%= f.label :text %><br/>
    <%= f.text_area :text %>
  <p>
    <%= f.submit 'Submit' %>
  </p>
<% end %>